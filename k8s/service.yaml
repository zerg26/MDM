apiVersion: v1
kind: Service
metadata:
  name: mdm-mcp
  labels:
    app: mdm-mcp
spec:
  selector:
    app: mdm-mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
